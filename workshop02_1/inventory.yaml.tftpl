# Generated on ${ts}

all:
  vars:
    ansible_user: root
    ansible_connection: ssh
    ansible_ssh_private_key_file: ${ssh_private_key_file}
    code_server_archive: ${code_server_archive}
    unpacked_directory: ${unpacked_directory}
    code_server_password: ${code_server_password}
    code_server_domain: ${code_server_domain}
  hosts:
    ${host_name}:
      ansible_host: ${droplet_ipv4}
